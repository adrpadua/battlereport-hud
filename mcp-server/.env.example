# Database Configuration
DATABASE_URL=postgresql://username:password@localhost:5432/wh40k_rules

# Firecrawl API Key (get from https://firecrawl.dev)
FIRECRAWL_API_KEY=your_firecrawl_api_key_here

# Optional: Rate limiting for scraping (requests per minute)
SCRAPE_RATE_LIMIT=10

# Optional: Cache directory for scraped content
CACHE_DIR=./.scrape-cache
